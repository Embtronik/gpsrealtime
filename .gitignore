# Secrets — never commit
.env

# Dependencies (managed by composer / pip)
vendor/

# Logs
logs/*.log

# Database backups (carpeta local, no subir)
backup/
# 01_restore.sql se sube al repo privado para seed automático en producción

# XAMPP / OS
*.DS_Store
Thumbs.db

# MySQL Workbench
*.mwb
*.mwb.bak

# Misc
usuarios.txt
guia_despliegue.txt
